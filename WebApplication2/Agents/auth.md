### In this realization is a missed web api users service which authenticates a user and gets its profile.
* Can you provide the missing pieces of authentication setup and user service which can response with users’ profile? 
* Let it be JWT authentication with a dedicated login page in angular app (client-app)